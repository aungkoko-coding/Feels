import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Message Details",
};

const MessageDetailLayout = ({ children }: { children: React.ReactNode }) => {
  return children;
};

export default MessageDetailLayout;
